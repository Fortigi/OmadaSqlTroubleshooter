<Window xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
 xmlns:local="clr-x:Namespace:Fortigi"
 xmlns:Wpf="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
 Title="Fortigi Omada Enterprise Cloud Sql Troubleshooter" Height="1020" Width="1140" MinWidth="1140" MinHeight="1020"  Background="{DynamicResource {x:Static SystemColors.ScrollBarBrushKey}}" BorderBrush="{DynamicResource {x:Static SystemColors.ActiveBorderBrushKey}}">

    <Window.Resources>
        <!-- Default TextBox Style -->
        <Style TargetType="TextBox">
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="Width" Value="300"/>
            <Setter Property="Background" Value="{DynamicResource {x:Static SystemColors.WindowBrushKey}}"/>
        </Style>
        <Style TargetType="Label">
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="Width" Value="160"/>
        </Style>
        <Style TargetType="Separator">
            <Setter Property="Background" Value="Transparent"/>
        </Style>
        <Style TargetType="Button">
            <Setter Property="Height" Value="30"/>
            <Setter Property="Width" Value="120"/>
            <Setter Property="IsEnabled" Value="False"/>
        </Style>
        <Style TargetType="ComboBox">
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="Width" Value="300"/>
        </Style>
    </Window.Resources>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Vertical" Grid.Row="0">
            <StackPanel Orientation="Horizontal" >
                <Label x:Name="LabelUrl" Content="Omada Tenant:"  HorizontalAlignment="Left"/>
                <Separator Width="20" />
                <TextBox x:Name="TextBoxURL" Width="300" Height="30" ToolTip="Enter full URL or tenant name only. Example: Enter 'comp' for 'https://comp.omada.cloud'" />
                <Separator Width="20" />
                <Label x:Name="LabelUserName" Content="Username:" Width="120" Height="30" Visibility="Hidden" />
                <Separator Width="20" />
                <TextBox x:Name="TextBoxUserName" Width="300" Height="30" Visibility="Hidden" IsEnabled="False"  />
            </StackPanel>
            <Separator Height="10" />
            <StackPanel Orientation="Horizontal">
                <Label x:Name="LabelAuthenticationOption" Content="Authentication type:"   />
                <Separator Width="20" />
                <ComboBox x:Name="ComboBoxSelectAuthenticationOption" SelectedIndex="0" >
                    <ComboBoxItem Content="Browser"/>
                    <!-- Not suitable for Omada Cloud
                        <ComboBoxItem Content="Integrated"/>
                        <ComboBoxItem Content="Basic"/>
                        <ComboBoxItem Content="Windows"/>
                    -->
                    <ComboBoxItem Content="OAuth"/>
                </ComboBox>
                <Separator Width="20" />
                <Label x:Name="LabelPassword" Content="Password:" Width="120" Height="30" Visibility="Hidden"  />
                <Separator Width="20" />
                <PasswordBox x:Name="TextBoxPassword" Width="300" Height="30" Visibility="Hidden"  />
            </StackPanel>
            <Separator Height="10" />
            <StackPanel Orientation="Horizontal">
                <Label x:Name="LabelComboBoxSelectQuery" Content="Query List:"    />
                <Separator Width="20" />
                <ComboBox x:Name="ComboBoxSelectQuery" />
                <Separator Width="20" />
                <CheckBox x:Name="CheckboxMyQueriesOnly" Content="Filter My Queries Only" VerticalAlignment="Center" ToolTip="Check this box to show only SQL queries that are created or last updated by you." />
            </StackPanel>
            <Separator Height="10" />
            <StackPanel Orientation="Horizontal">
                <Label x:Name="LabelDisplayName" Content="Query Display Name:" ToolTip="The Display Name of a a query, the display name can be changed if needed." />
                <Separator Width="20" />
                <TextBox x:Name="TextBoxDisplayName" IsEnabled="False"  />
                <Separator Width="20" />
                <Label x:Name="LabelDataConnection" Content="Data Connection:"/>
                <Separator Width="20" />
                <!-- <TextBox x:Name="TextBoxDataConnection"  IsEnabled="False" /> -->
                <ComboBox x:Name="ComboBoxSelectDataConnection"/>
            </StackPanel>
            <Separator Height="10" />
        </StackPanel>

        <StackPanel Orientation="Horizontal" Grid.Row="1">
            <Button x:Name="ButtonRefreshQueries" Content="Refresh Query _List" IsEnabled="True" />
            <Separator Width="20" />
            <Button x:Name="ButtonSaveQuery" Content="_Save Query" />
            <Separator Width="20" />
            <Button x:Name="ButtonExecuteQuery" Content="_Execute Query" />
            <Separator Width="20" />
            <Button x:Name="ButtonShowOutput" Content="_View Data" />
            <Separator Width="20" />
            <Button x:Name="ButtonSaveOutputFile" Content="Export _Data" />
            <Separator Width="20" />
            <Button x:Name="ButtonOpenOutputFile" Content="Open Export _File" />
            <Separator Width="20" />
            <Button x:Name="ButtonReset" Content="_Reset" IsEnabled="True" />
            <Separator Width="20" />
            <Button x:Name="ButtonClearLog" Content="_Clear Log" IsEnabled="True" />
        </StackPanel>
        <Separator Height="10" />

        <Grid Grid.Row="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <StackPanel Orientation="Vertical" Grid.Row="0">
                <Label x:Name="LabelSqlEditor" Content="SQL Query Editor" Width="120" Height="25" HorizontalAlignment="Left" VerticalAlignment="Center" />
            </StackPanel>
            <Border Grid.Row="1" BorderBrush="Gray" BorderThickness="1" Background="{DynamicResource {x:Static SystemColors.AppWorkspaceBrushKey}}">
                <Wpf:WebView2 x:Name="webView21" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" MinWidth="980" MinHeight="400"/>
            </Border>
        </Grid>

        <StackPanel Orientation="Vertical" Grid.Row="3" HorizontalAlignment="Stretch" VerticalAlignment="Bottom">
            <Separator Height="10"  />
            <StackPanel Orientation="Vertical">
                <Label x:Name="LabelOutputBox" Content="Output" Width="120" Height="25" HorizontalAlignment="Left" VerticalAlignment="Center" />
                <TextBox x:Name="TextBoxOutput" AcceptsReturn="True" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto" FontFamily="Consolas" IsReadOnly="True" HorizontalAlignment="Stretch" MinHeight="200" MinWidth="1120" VerticalAlignment="Top" Background="{DynamicResource {x:Static SystemColors.WindowBrushKey}}" Width="NaN" VerticalContentAlignment="Top" />
            </StackPanel>
            <Separator Height="10" />
            <StackPanel Orientation="Horizontal">
                <Label x:Name="LabelSelectLogLevel" Content="Log Level:"  HorizontalAlignment="Left" Width="Auto"/>
                <Separator Width="10" />
                <ComboBox x:Name="ComboBoxSelectLogLevel" SelectedIndex="0" HorizontalAlignment="Left" VerticalAlignment="Center" Width="80">
                    <ComboBoxItem Content="INFO"/>
                    <ComboBoxItem Content="ERROR"/>
                    <ComboBoxItem Content="DEBUG"/>
                    <ComboBoxItem Content="VERBOSE"/>
                </ComboBox>
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>

