<Window xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:local="clr-x:Namespace:Fortigi" Title="Fortigi Omada Sql Troubleshooter" Background="{DynamicResource {x:Static SystemColors.ScrollBarBrushKey}}" x:Name="MainForm" BorderBrush="#FFC8C8C8" SizeToContent="Manual">

        <Window.Style>
                <Style TargetType="Window">
                        <Setter Property="MinWidth" Value="1461" />
                        <Setter Property="Width" Value="1461" />
                        <Setter Property="MinHeight" Value="800" />
                        <Setter Property="Height" Value="800" />
                </Style>
        </Window.Style>

        <Grid x:Name="MainWindowGrid">
                <!-- SelectedIndex=-1 is required: with only TabItemAddNew present at load time,
                     a TabControl otherwise auto-selects it as soon as the control materializes -
                     well before MainForm.Definition's Add_Loaded/Restore-TabSessions runs - which
                     fires New-TabSession reentrantly against the still-loading MainForm. -->
                <TabControl x:Name="TabControlSessions" BorderThickness="0" Padding="0" SelectedIndex="-1">
                        <TabItem x:Name="TabItemAddNew" Header="+" ToolTip="Open a new tab" />
                </TabControl>
        </Grid>
</Window>
