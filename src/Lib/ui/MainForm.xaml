<Window xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:local="clr-x:Namespace:Fortigi" Title="Fortigi Omada Sql Troubleshooter" Background="{DynamicResource {x:Static SystemColors.ScrollBarBrushKey}}" x:Name="MainForm" BorderBrush="#FFC8C8C8" SizeToContent="Manual">

        <Window.Style>
                <Style TargetType="Window">
                        <Setter Property="MinWidth" Value="1461" />
                        <Setter Property="Width" Value="1461" />
                        <Setter Property="MinHeight" Value="800" />
                        <Setter Property="Height" Value="800" />
                </Style>
        </Window.Style>

        <Window.Resources>
                <!-- Close ("x") button inside each tab header (built in New-TabHeaderControl).
                     Flat by default, Windows-style red highlight on hover. -->
                <Style x:Key="TabCloseButtonStyle" TargetType="Button">
                        <Setter Property="Width" Value="18" />
                        <Setter Property="Height" Value="18" />
                        <Setter Property="Padding" Value="0" />
                        <Setter Property="FontSize" Value="10" />
                        <Setter Property="Foreground" Value="#6B7280" />
                        <Setter Property="Background" Value="Transparent" />
                        <Setter Property="BorderThickness" Value="0" />
                        <Setter Property="Cursor" Value="Hand" />
                        <Setter Property="Template">
                                <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                                <Border x:Name="CloseBorder" Background="{TemplateBinding Background}" CornerRadius="3" SnapsToDevicePixels="True">
                                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                                                </Border>
                                                <ControlTemplate.Triggers>
                                                        <Trigger Property="IsMouseOver" Value="True">
                                                                <Setter TargetName="CloseBorder" Property="Background" Value="#E81123" />
                                                                <Setter Property="Foreground" Value="White" />
                                                        </Trigger>
                                                </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                </Setter.Value>
                        </Setter>
                </Style>

                <!-- SSMS-style session tabs: muted when inactive, light with a blue accent bar on
                     top when active (blue = the app frame colour #FF293955). Implicit style so both
                     the session tabs and the "+" add tab pick it up automatically. -->
                <Style TargetType="TabItem">
                        <Setter Property="Foreground" Value="#FF404040" />
                        <Setter Property="Template">
                                <Setter.Value>
                                        <ControlTemplate TargetType="TabItem">
                                                <Border x:Name="TabBorder" Margin="0,2,2,0" Background="#FFD6DBE0" BorderBrush="#FFB8C0C9" BorderThickness="1,1,1,0" CornerRadius="5,5,0,0" SnapsToDevicePixels="True">
                                                        <Border x:Name="AccentBorder" BorderBrush="Transparent" BorderThickness="0,2,0,0" CornerRadius="5,5,0,0">
                                                                <ContentPresenter x:Name="ContentSite" ContentSource="Header" Margin="10,3,8,4" VerticalAlignment="Center" HorizontalAlignment="Left" />
                                                        </Border>
                                                </Border>
                                                <ControlTemplate.Triggers>
                                                        <Trigger Property="IsSelected" Value="True">
                                                                <Setter TargetName="TabBorder" Property="Background" Value="#FFF4F5F7" />
                                                                <Setter TargetName="TabBorder" Property="Margin" Value="0,0,2,0" />
                                                                <Setter TargetName="AccentBorder" Property="BorderBrush" Value="#FF293955" />
                                                                <Setter Property="Panel.ZIndex" Value="1" />
                                                                <Setter Property="Foreground" Value="#FF1F1F1F" />
                                                        </Trigger>
                                                        <MultiTrigger>
                                                                <MultiTrigger.Conditions>
                                                                        <Condition Property="IsSelected" Value="False" />
                                                                        <Condition Property="IsMouseOver" Value="True" />
                                                                </MultiTrigger.Conditions>
                                                                <Setter TargetName="TabBorder" Property="Background" Value="#FFE4E8EC" />
                                                        </MultiTrigger>
                                                </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                </Setter.Value>
                        </Setter>
                </Style>
        </Window.Resources>

        <Grid x:Name="MainWindowGrid">
                <!-- SelectedIndex=-1 is required: with only TabItemAddNew present at load time,
                     a TabControl otherwise auto-selects it as soon as the control materializes -
                     well before MainForm.Definition's Add_Loaded/Restore-TabSessions runs - which
                     fires New-TabSession reentrantly against the still-loading MainForm. -->
                <TabControl x:Name="TabControlSessions" BorderThickness="0" Padding="0" Background="Transparent" SelectedIndex="-1">
                        <TabItem x:Name="TabItemAddNew" Header="+" ToolTip="Open a new tab" />
                </TabControl>
        </Grid>
</Window>
